import 'package:flutter/material.dart';

// de momento stateless solo para agregar un color
class ProfileTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
    );
  }
}
